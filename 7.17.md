**定理 1.2阅读：** 设 $(X, d_X)$ 为 $\mathbb{R}^d$ 的某个子集上的度量空间。假设 $(X, d_X)$ 存在一个 $(r, cr, p, q)$-敏感哈希族 $\mathcal{H}$，记 $\rho = \frac{\log(1/p)}{\log(1/q)}$。那么对于任意 $n \ge \frac{1}{q}$，存在一个随机化算法，用于处理 $X$ 中 $n$ 个点构成的子集上的 $(r, cr)$-近邻问题，其时空复杂度如下：

- **空间复杂度：** $O(dn + n^{1+\rho})$
- **查询时间复杂度：** $O(n^{\rho})$ 次距离计算 与 $O(n^{\rho} \log_{1/q} n)$ 次哈希函数评估

由此可见，$\rho$ 越小，算法的空间和查询时间开销就越低，性能越好。

---

**定理 1.3阅读：** 对于任意 $c, s \ge 1$，有
$$
\rho_s(c) \ge \frac{e^{1 / c^s} - 1}{e^{1 / c^s} + 1} \ge \frac{e - 1}{e + 1} \cdot \frac{1}{c^s} \ge \frac{0.462}{c^s}
$$

我们可以设$1/C^S$=x,那么对函数f(x)=$\frac{(e^x-1)}{(e^x+1)}$进行分析，进行二次求导，f‘’(x)=$\frac{2e^x}{(e^x+1)^2}*(1-(e^2)^x)$

由于x>0，所以该二阶导恒小于0，且$c^s$>=1,所以0<=x<=1，那么f(0)=0,f(1)=$\frac{e-1}{e+1}$,所以f(x)恒大于这两点所连接的弦，即f(x)=$\frac{e-1}{e+1}*x$，所以第二个不等号成立，对e取近似值可得到第三个不等号，那么我们只需要证明第一个不等号，后面的自然成立。



**定理 1.3 的证明思路**

定理 1.3 证明的基本思想很简单。随机选取一点 $x \in \{0,1\}^d$，并考虑超立方体 $\{0,1\}^d$ 中由满足 $\mathcal{H}(u) = \mathcal{H}(x)$ 的点 $u$ 组成的随机子集 $A$。定义 1.1 中的第二个条件迫使 $A$ 在期望意义下很小。而当 $A$ 很小时，我们可以从上界估计从 $A$ 中随机一点出发，经过 $r$ 步随机游走后仍落在 $A$ 中的概率。我们利用傅里叶分析论证得到这个上界，并结合定义 1.1 中的第一个条件推导出所需的 $\rho_1(c)$ 的界。

定理1.3可以根据以下结果得出

**命题 2.1阅读** 设 $\mathcal{H}$ 是汉明立方体 $(\{0,1\}^d, \|\cdot\|_1)$ 上的一个 $(r, R, p, q)$-敏感哈希族。假设 $r$ 是奇数且 $R < d/2$，那么
$$
p \le \left( q + e^{-\frac{1}{2}\left(\frac{d}{2} - R\right)^2} \right)^{\frac{2r/d - 1}{2r/d + 1}}.
$$

在命题 2.1 中取 $R \approx \frac{d}{2} - \sqrt{d \log d}$ 以及 $r \approx R/c$，并令 $d \to \infty$，就得到 $s = 1$ 情形的定理 1.3。对于一般的 $s \ge 1$ 的情形，可由对任意 $x, y \in \{0,1\}^d$ 有 $\|x - y\|_s = \|x - y\|_1^{1/s}$ 这一事实得出。

**注 2.1.** 对于 $(\{0,1\}^d, \|\cdot\|_1)$ 上的任意 $(r, cr, p, q)$-敏感哈希族，即使允许 $q$ 依赖于 $d$，命题 2.1 也给出了 $\frac{\log(1/p)}{\log(1/q)}$ 的非平凡下界。注意到根据 (1) 中给出的定义，定理 1.3 仅对常数 $q$ 蕴含这样的下界。但命题 2.1 要强得多，它对任意 $q \ge 2^{-o(d)}$ 给出的界与 1.3 中的下界渐近重合。



汉明立方体：所有长度为d，由{0,1}组成的字符串，若维度为3，则包含八个点，分别为三维空间中单位立方体的八个顶点。



命题 2.1 的证明将分解为几个引理。



**引理 2.2** 设 $H$ 是汉明立方体 $(\{0,1\}^d, \|\cdot\|_1)$ 上的一个 $(r,R,p,q)$-敏感哈希族，并固定 $x \in \{0,1\}^d$。则
$$
\mathbb{E} \bigl|H^{-1}(H(x))\bigr| \le \sum_{k=0}^{\lfloor R \rfloor} \binom{d}{k} + q \cdot \sum_{k=\lfloor R \rfloor + 1}^{d} \binom{d}{k}.
$$


H(x)表示x映射到的位置，$H^-1$(y)表示映射到位置y上的点，那么$H^-1(H(x))$表示映射位置和x相同的点，在d维汉明立方体中，一个点和另一个点的距离表示为他们两个之间的不同位数，所以固定一个点，距离他为k的点的个数为$\binom{d}{k}$,相当于从d个数中挑出k位进行取反，设距离该点距离大于r小于R的点映射到这个点的概率为p'，那么不超过R的点的个数总和可以表示为$p\sum_{k=0}^{r}\binom{d}{k}+p'\sum_{k=r+1}^{R}\binom{d}{k}+q\sum_{k=R+1}^{d}\binom{d}{k}$,由于p<=1，且p'<=1，所以对p，p'取1，$\mathbb{E} \bigl|H^{-1}(H(x))\bigr|<=$$\sum_{k=0}^{R}\binom{d}{k}+q\sum_{k=R+1}^{d}\binom{d}{k}$



**推论 2.3.** 假设 $R < \frac{d}{2}$。我们有
$$
\mathbb{E}\left|\mathcal{H}^{-1}(\mathcal{H}(x))\right| \le 2^d \left( q + e^{-\frac{1}{d}\left(\frac{d}{2} - R\right)^2} \right).
$$

**证明：** 这由引理 2.2 以及标准估计
$$
\sum_{k \le \frac{d}{2} - a} \binom{d}{k} \le 2^d \cdot e^{-\frac{a^2}{d}}
$$

推出。



标准估计的证明：